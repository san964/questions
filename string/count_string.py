# Take a string as input from the user
user_input = input("Enter a string: ")

# Calculate the length of the string
count_of_characters = len(user_input)

# Display the result
print("The count of characters in the string is:", count_of_characters)
